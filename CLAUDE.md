# CLAUDE.md

## 규칙

1. 시키지 않은 것은 하지 마라.
2. 작업 전 **`docs/` 문서를 숙지**한다 — 최근 `docs/history/`(다른 사람 작업 싱크) + `docs/ops/`(운영 레퍼런스).

## 기능정의서 (기획 전체)

`docs/기능정의서_v*.md` 는 **이 프로젝트가 만들 기능 전체를 정의한 현행 기획서**다. **항상 최신 버전을 본다 (현재 `docs/기능정의서_v1.2.md`).**

- **TODO/작업 목록이 아니다.** "무엇을 만드는가(요건)"를 규정한 단일 기준 문서다 — 스크리너·로그인/회원·공통·모의투자(솔로)·모의투자 대결 전 영역을 포함한다.
- 작업 목록·진행 순서는 `docs/작업슬러그_v1.1.md`, 실제 실행 기록은 `docs/history/` 에 따로 있다.
- 기능 작업 전 해당 영역 요건을 이 문서에서 먼저 확인하고, 요건이 바뀌면 **새 버전(vN+1)으로 갱신**한다(완결 문서로 — 이전 내용 누락 금지).

## 실행한 작업 md 관리 (`docs/history/`)

개발은 **작업 내용을 md로 작성 → 그 md를 Claude Code로 실행 → 다음 작업 md 작성 → 실행** 패턴으로 진행한다.
`docs/history/`에는 **실제로 실행한 그 md(작업 지시서/계획서)** 를 보관한다. 3명이 각자 작업하므로, 이 md들을 통해 서로 무엇을 실행했는지 싱크한다.

### 작업 전 — 싱크

- 작업 시작 전 `docs/history/` 하위의 최근 md들을 읽어, 다른 사람이 무엇을 실행했고 현재 어디까지 진행됐는지 파악한 뒤 진행한다.
- 파일명이 날짜순으로 정렬되니 최신 파일부터 확인한다.

### 실행한 md 보관 규칙

- 실행한 작업 md 1개 = 파일 1개로 `docs/history/`에 보관한다.
- **파일명**: `YYYY-MM-DD-<git아이디>-<작업슬러그>.md`
  - 예: `2026-06-05-inkangdev-주가적재배치.md`
  - `<git아이디>`는 `git config user.name` 값을 사용한다.

## 참고 문서

작업 전 아래 문서를 **숙지**한다(규칙 2).

- `docs/ops/셀프호스팅.md` — 셀프호스팅(내 PC = Render+Supabase 대체) 배포/운영 레퍼런스.
